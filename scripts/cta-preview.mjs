import {chromium} from '@playwright/test'; const b=await chromium.launch();const p=await b.newPage({viewport:{width:1440,height:1000}});await p.goto('http://127.0.0.1:4173/tr/blog');await p.locator('.cta-banner').scrollIntoViewIfNeeded();await p.waitForTimeout(500);console.log(await p.locator('.cta-banner img').evaluate(e=>({html:e.outerHTML,natural:e.naturalWidth,rect:e.getBoundingClientRect().toJSON(),opacity:getComputedStyle(e).opacity,display:getComputedStyle(e).display})));await p.locator('.cta-banner').screenshot({path:'reports/redesign/screenshots/cta-check.png'});await b.close();
